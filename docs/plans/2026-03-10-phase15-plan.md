# Phase 15: 운영 + 수익화 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SEO, 댓글(Giscus), AdSense 사이드바, 커스텀 도메인 설정, favicon을 구현하여 블로그 운영 인프라를 완성한다.

**Architecture:** Next.js App Router 규약 파일(sitemap.ts, robots.ts, icon.svg)과 `next/og`를 활용한 동적 OG 이미지 생성. Giscus는 Client Component로 테마 연동. AdSense는 Tech 페이지에 2컬럼 레이아웃을 도입하여 사이드바에 배치.

**Tech Stack:** Next.js 15 App Router, next/og (Satori), Giscus, Google AdSense, Tailwind CSS v4

---

## Task 1: Favicon — `>_` 터미널 커서 SVG

**Files:**
- Create: `src/app/icon.svg`

- [ ] **Step 1: SVG favicon 생성**

`src/app/icon.svg` — `>_` 터미널 커서, amber(#fbbf24) on dark(#070709) 배경, 32x32 viewBox.

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="4" fill="#070709"/>
  <text x="3" y="23" font-family="monospace" font-size="18" font-weight="bold" fill="#fbbf24">&gt;_</text>
</svg>
```

- [ ] **Step 2: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공, favicon 자동 인식

- [ ] **Step 3: 커밋**

```bash
git add src/app/icon.svg
git commit -m "feat: >_ 터미널 커서 favicon 추가"
```

---

## Task 2: SEO — sitemap.xml + robots.txt

**Files:**
- Create: `src/app/sitemap.ts`
- Create: `src/app/robots.ts`

- [ ] **Step 1: sitemap.ts 생성**

`src/app/sitemap.ts` — Next.js App Router 규약 파일. `getAllPosts()`와 `getAllLogs()`로 동적 URL 생성.

```typescript
import type { MetadataRoute } from "next";
import { getAllPosts } from "@/lib/posts";
import { getAllLogs } from "@/lib/logs";

const BASE_URL = "https://deep-thought.dev";

const sitemap = (): MetadataRoute.Sitemap => {
  const posts = getAllPosts();
  const logs = getAllLogs();

  const staticPages: MetadataRoute.Sitemap = [
    { url: BASE_URL, lastModified: new Date(), changeFrequency: "daily", priority: 1 },
    { url: `${BASE_URL}/tech`, lastModified: new Date(), changeFrequency: "daily", priority: 0.9 },
    { url: `${BASE_URL}/profile`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.5 },
    { url: `${BASE_URL}/portfolio`, lastModified: new Date(), changeFrequency: "monthly", priority: 0.5 },
    { url: `${BASE_URL}/log`, lastModified: new Date(), changeFrequency: "weekly", priority: 0.7 },
  ];

  const postPages: MetadataRoute.Sitemap = posts.map((post) => ({
    url: `${BASE_URL}/tech/${post.slug}`,
    lastModified: new Date(post.date),
    changeFrequency: "monthly" as const,
    priority: 0.8,
  }));

  const logPages: MetadataRoute.Sitemap = logs.map((log) => ({
    url: `${BASE_URL}/log/${log.slug}`,
    lastModified: new Date(log.date),
    changeFrequency: "monthly" as const,
    priority: 0.6,
  }));

  return [...staticPages, ...postPages, ...logPages];
};

export default sitemap;
```

- [ ] **Step 2: robots.ts 생성**

`src/app/robots.ts` — 전체 허용 + sitemap 위치 명시.

```typescript
import type { MetadataRoute } from "next";

const robots = (): MetadataRoute.Robots => ({
  rules: {
    userAgent: "*",
    allow: "/",
  },
  sitemap: "https://deep-thought.dev/sitemap.xml",
});

export default robots;
```

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공

- [ ] **Step 4: 커밋**

```bash
git add src/app/sitemap.ts src/app/robots.ts
git commit -m "feat: sitemap.xml + robots.txt 동적 생성 (SEO)"
```

---

## Task 3: SEO — 메타데이터 강화 + JSON-LD

**Files:**
- Modify: `src/app/layout.tsx`
- Modify: `src/app/tech/[slug]/page.tsx`
- Modify: `src/app/log/[slug]/page.tsx`

- [ ] **Step 1: 루트 layout.tsx 메타데이터 확장**

`src/app/layout.tsx` — 기존 `metadata` 객체를 **교체**. metadataBase, OG 기본값, robots 추가.

```typescript
// 기존 metadata를 아래로 완전 교체
export const metadata: Metadata = {
  metadataBase: new URL("https://deep-thought.dev"),
  title: {
    default: "Deep Thought",
    template: "%s | Deep Thought",
  },
  description: "The answer to the ultimate question of life, the universe, and code.",
  openGraph: {
    type: "website",
    locale: "ko_KR",
    siteName: "Deep Thought",
    title: "Deep Thought",
    description: "The answer to the ultimate question of life, the universe, and code.",
  },
  robots: {
    index: true,
    follow: true,
  },
};
```

- [ ] **Step 2: 포스트 상세 generateMetadata 확장**

`src/app/tech/[slug]/page.tsx` — OG 이미지 + article 타입 + JSON-LD 추가.

```typescript
export const generateMetadata = async ({
  params,
}: {
  params: Promise<{ slug: string }>;
}): Promise<Metadata> => {
  const { slug } = await params;
  const post = await getPostBySlug(slug);

  if (!post) {
    return { title: "Post Not Found" };
  }

  return {
    title: post.title,
    description: post.summary,
    openGraph: {
      type: "article",
      title: post.title,
      description: post.summary,
      publishedTime: post.date,
      authors: [post.author],
      tags: post.tags,
      images: [
        {
          url: `/api/og?title=${encodeURIComponent(post.title)}&category=${encodeURIComponent(post.category)}`,
          width: 1200,
          height: 630,
          alt: post.title,
        },
      ],
    },
  };
};
```

JSON-LD는 페이지 JSX 내부 `<script type="application/ld+json">` 로 삽입:

```typescript
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "Article",
  headline: post.title,
  description: post.summary,
  datePublished: post.date,
  author: { "@type": "Person", name: post.author },
  publisher: { "@type": "Organization", name: "Deep Thought" },
};
```

```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
/>
```

- [ ] **Step 3: 로그 상세 메타데이터 추가**

`src/app/log/[slug]/page.tsx` — `generateMetadata` 함수 추가.

```typescript
import type { Metadata } from "next";

export const generateMetadata = async ({
  params,
}: {
  params: Promise<{ slug: string }>;
}): Promise<Metadata> => {
  const { slug } = await params;
  const log = await getLogBySlug(slug);

  if (!log) {
    return { title: "Log Not Found" };
  }

  return {
    title: log.title,
    description: log.summary,
    openGraph: {
      type: "article",
      title: log.title,
      description: log.summary,
      publishedTime: log.date,
      tags: log.tags,
    },
  };
};
```

- [ ] **Step 4: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공

- [ ] **Step 5: 커밋**

```bash
git add src/app/layout.tsx src/app/tech/\[slug\]/page.tsx src/app/log/\[slug\]/page.tsx
git commit -m "feat: 메타데이터 강화 + JSON-LD 구조화 데이터 (SEO)"
```

---

## Task 4: SEO — OG 이미지 동적 생성

**Files:**
- Create: `src/app/api/og/route.tsx`

- [ ] **Step 1: OG 이미지 route handler 생성**

`src/app/api/og/route.tsx` — `next/og`의 `ImageResponse`를 사용. 쿼리 파라미터로 title, category를 받아 브랜드 스타일 이미지 생성.

```tsx
import { ImageResponse } from "next/og";
import type { NextRequest } from "next/server";

export const runtime = "edge";

export const GET = async (request: NextRequest) => {
  const { searchParams } = new URL(request.url);
  const title = searchParams.get("title") ?? "Deep Thought";
  const category = searchParams.get("category") ?? "";

  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          display: "flex",
          flexDirection: "column",
          justifyContent: "center",
          padding: "60px 80px",
          backgroundColor: "#070709",
          color: "#fafaf9",
          fontFamily: "monospace",
        }}
      >
        {/* Top: Brand */}
        <div style={{ display: "flex", alignItems: "center", gap: "12px" }}>
          <span style={{ fontSize: "24px", color: "#fbbf24", fontWeight: 700 }}>
            &gt;_
          </span>
          <span style={{ fontSize: "20px", color: "#a8a29e", letterSpacing: "0.15em" }}>
            DEEP THOUGHT
          </span>
        </div>

        {/* Title */}
        <div
          style={{
            marginTop: "40px",
            fontSize: "48px",
            fontWeight: 700,
            lineHeight: 1.2,
            maxWidth: "900px",
            wordBreak: "break-word",
          }}
        >
          {title}
        </div>

        {/* Category badge */}
        {category && (
          <div
            style={{
              marginTop: "32px",
              display: "flex",
            }}
          >
            <span
              style={{
                fontSize: "16px",
                color: "#fbbf24",
                border: "1px solid #fbbf2440",
                borderRadius: "6px",
                padding: "4px 16px",
                letterSpacing: "0.1em",
              }}
            >
              {category}
            </span>
          </div>
        )}

        {/* Bottom tagline */}
        <div
          style={{
            position: "absolute",
            bottom: "40px",
            left: "80px",
            fontSize: "14px",
            color: "#78716c",
            letterSpacing: "0.1em",
          }}
        >
          The answer to the ultimate question of life, the universe, and code.
        </div>
      </div>
    ),
    { width: 1200, height: 630 }
  );
};
```

- [ ] **Step 2: 로컬 테스트**

Run: `npm run dev`
브라우저에서 `http://localhost:3000/api/og?title=Spring+Security&category=Backend` 접속하여 이미지 확인.

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공

- [ ] **Step 4: 커밋**

```bash
git add src/app/api/og/route.tsx
git commit -m "feat: OG 이미지 동적 생성 API (next/og Satori)"
```

---

## Task 5: Giscus 댓글 + 좋아요

**Files:**
- Create: `src/components/giscus-comments.tsx`
- Modify: `src/app/tech/[slug]/page.tsx`

**사전 작업 (수동):**
1. GitHub 리포지토리 Settings → Features → Discussions 활성화
2. https://giscus.app 에서 설정 생성 (repo: ZANNABI-LAB/sunny-blog, mapping: pathname, reactions 활성화)
3. 생성된 `data-repo-id`, `data-category-id` 값 확인

- [ ] **Step 1: Giscus 컴포넌트 생성**

`src/components/giscus-comments.tsx` — Client Component, next-themes 테마 연동.

```tsx
"use client";

import { useEffect, useRef } from "react";
import { useTheme } from "next-themes";

const GISCUS_CONFIG = {
  repo: "ZANNABI-LAB/sunny-blog" as `${string}/${string}`,
  repoId: "", // giscus.app에서 확인 후 입력
  category: "Announcements",
  categoryId: "", // giscus.app에서 확인 후 입력
};

const GiscusComments = () => {
  const ref = useRef<HTMLDivElement>(null);
  const { resolvedTheme } = useTheme();

  useEffect(() => {
    if (!ref.current) return;

    const giscusTheme = resolvedTheme === "dark" ? "dark_dimmed" : "light";

    // 기존 iframe이 있으면 테마만 업데이트
    const existingIframe = ref.current.querySelector<HTMLIFrameElement>(
      "iframe.giscus-frame"
    );
    if (existingIframe) {
      existingIframe.contentWindow?.postMessage(
        { giscus: { setConfig: { theme: giscusTheme } } },
        "https://giscus.app"
      );
      return;
    }

    const script = document.createElement("script");
    script.src = "https://giscus.app/client.js";
    script.setAttribute("data-repo", GISCUS_CONFIG.repo);
    script.setAttribute("data-repo-id", GISCUS_CONFIG.repoId);
    script.setAttribute("data-category", GISCUS_CONFIG.category);
    script.setAttribute("data-category-id", GISCUS_CONFIG.categoryId);
    script.setAttribute("data-mapping", "pathname");
    script.setAttribute("data-strict", "0");
    script.setAttribute("data-reactions-enabled", "1");
    script.setAttribute("data-emit-metadata", "0");
    script.setAttribute("data-input-position", "top");
    script.setAttribute("data-theme", giscusTheme);
    script.setAttribute("data-lang", "ko");
    script.setAttribute("data-loading", "lazy");
    script.crossOrigin = "anonymous";
    script.async = true;

    ref.current.appendChild(script);
  }, [resolvedTheme]);

  return <div ref={ref} className="mt-12 pt-8 border-t border-border" />;
};

export default GiscusComments;
```

- [ ] **Step 2: 포스트 상세에 댓글 컴포넌트 배치**

`src/app/tech/[slug]/page.tsx` — 하단 목록 링크 위에 `<GiscusComments />` 추가.

```tsx
import GiscusComments from "@/components/giscus-comments";

// ... 기존 코드 ...

{/* 댓글 */}
<GiscusComments />

{/* 하단 목록 링크 */}
<div className="mt-12 border-t border-border pt-6 pb-8">
```

- [ ] **Step 3: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공

- [ ] **Step 4: 커밋**

```bash
git add src/components/giscus-comments.tsx src/app/tech/\[slug\]/page.tsx
git commit -m "feat: Giscus 댓글 + 리액션(좋아요) 추가"
```

---

## Task 6: AdSense — 사이드바 레이아웃 + 광고 컴포넌트

**Files:**
- Create: `src/components/ad-unit.tsx`
- Create: `src/components/sidebar-layout.tsx`
- Modify: `src/app/tech/page.tsx`
- Modify: `src/app/tech/[slug]/page.tsx`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: 광고 슬롯 컴포넌트 생성**

`src/components/ad-unit.tsx` — AdSense 광고 영역. 승인 전에는 placeholder 표시.

```tsx
"use client";

import { useEffect, useRef } from "react";

type AdUnitProps = {
  slot: string;
  className?: string;
};

const AdUnit = ({ slot, className = "" }: AdUnitProps) => {
  const adRef = useRef<HTMLModElement>(null);

  useEffect(() => {
    if (typeof window !== "undefined" && adRef.current) {
      try {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        ((window as any).adsbygoogle = (window as any).adsbygoogle || []).push({});
      } catch {
        // AdSense not loaded yet
      }
    }
  }, []);

  const adClientId = process.env.NEXT_PUBLIC_ADSENSE_CLIENT_ID;

  if (!adClientId) {
    // AdSense 미설정 시 placeholder
    return (
      <div
        className={`rounded-md border border-dashed border-border p-4 text-center text-xs text-text-muted font-display ${className}`}
      >
        AD SPACE
      </div>
    );
  }

  return (
    <ins
      ref={adRef}
      className={`adsbygoogle block ${className}`}
      data-ad-client={adClientId}
      data-ad-slot={slot}
      data-ad-format="auto"
      data-full-width-responsive="true"
    />
  );
};

export default AdUnit;
```

- [ ] **Step 2: 사이드바 레이아웃 컴포넌트 생성**

`src/components/sidebar-layout.tsx` — 콘텐츠 + 사이드바 2컬럼 레이아웃. lg 이상에서만 사이드바 표시.

```tsx
import AdUnit from "@/components/ad-unit";

type SidebarLayoutProps = {
  children: React.ReactNode;
  adSlot?: string;
};

const SidebarLayout = ({ children, adSlot = "default" }: SidebarLayoutProps) => (
  <div className="max-w-5xl lg:max-w-7xl mx-auto lg:flex lg:gap-8">
    <div className="flex-1 min-w-0">{children}</div>
    <aside className="hidden lg:block w-[200px] shrink-0 sticky top-20 self-start">
      <AdUnit slot={adSlot} />
    </aside>
  </div>
);

export default SidebarLayout;
```

- [ ] **Step 3: Tech 목록 페이지에 사이드바 레이아웃 적용**

`src/app/tech/page.tsx` — 기존 `max-w-5xl` 래퍼를 `SidebarLayout`으로 교체.

기존:
```tsx
<div className="max-w-5xl mx-auto">
  {/* 기존 내용 */}
</div>
```

변경:
```tsx
<SidebarLayout adSlot="tech-list">
  <div>
    {/* 기존 내용 (max-w-5xl 제거, SidebarLayout이 max-w-7xl 관리) */}
  </div>
</SidebarLayout>
```

- [ ] **Step 4: Tech 상세 페이지에 사이드바 레이아웃 적용**

`src/app/tech/[slug]/page.tsx` — 기존 `max-w-4xl` 래퍼를 `SidebarLayout`으로 교체.

기존:
```tsx
<div className="max-w-4xl mx-auto animate-page-fade-in">
```

변경:
```tsx
<SidebarLayout adSlot="tech-detail">
  <div className="animate-page-fade-in">
```

- [ ] **Step 5: AdSense 스크립트 추가 (layout.tsx)**

`src/app/layout.tsx` — `next/script`로 AdSense 스크립트 조건부 로드. `<head>` 내부가 아닌 `<body>` 내부에 배치.

```tsx
import Script from "next/script";

// body 내부, ThemeProvider 바깥에 배치
{process.env.NEXT_PUBLIC_ADSENSE_CLIENT_ID && (
  <Script
    async
    src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${process.env.NEXT_PUBLIC_ADSENSE_CLIENT_ID}`}
    crossOrigin="anonymous"
    strategy="afterInteractive"
  />
)}
```

- [ ] **Step 6: 빌드 검증**

Run: `npm run build`
Expected: 빌드 성공

- [ ] **Step 7: 커밋**

```bash
git add src/components/ad-unit.tsx src/components/sidebar-layout.tsx src/app/tech/page.tsx src/app/tech/\[slug\]/page.tsx src/app/layout.tsx
git commit -m "feat: AdSense 사이드바 레이아웃 + 광고 컴포넌트"
```

---

## Task 7: 커스텀 도메인 설정 가이드

이 태스크는 코드 변경이 아닌 수동 설정 작업이다.

- [ ] **Step 1: Vercel에서 도메인 구매**

1. Vercel Dashboard → 프로젝트(sunny-blog) → Settings → Domains
2. `deep-thought.dev` 검색 → 구매
3. DNS/SSL 자동 설정 확인

- [ ] **Step 2: 도메인 반영 확인**

`https://deep-thought.dev` 접속하여 사이트 정상 작동 확인.
sitemap.xml, robots.txt에 도메인이 올바르게 표시되는지 확인.

- [ ] **Step 3: 커밋 (필요시)**

도메인 관련 코드 변경이 있으면 커밋. 없으면 스킵.

---

## 구현 순서 및 의존성

```
Task 1 (favicon)        ─── 독립
Task 2 (sitemap/robots) ─── 독립
Task 3 (메타데이터)      ─── Task 4 이전에 완료 (OG 이미지 URL 참조)
Task 4 (OG 이미지)       ─── Task 3 이후
Task 5 (Giscus)         ─── 독립 (사전 GitHub Discussions 활성화 필요)
Task 6 (AdSense)        ─── 독립
Task 7 (도메인)          ─── Task 2 이후 (sitemap에 도메인 하드코딩)
```

병렬 가능 그룹:
- **그룹 A**: Task 1, Task 2, Task 5 (완전 독립)
- **그룹 B**: Task 3 → Task 4 (순차)
- **그룹 C**: Task 6 (독립)
- **수동**: Task 7 (Vercel 설정)
